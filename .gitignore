target/
.worktrees/
artifacts/

# Disposable design-review HTML (design-in-practice skill output). Regenerable; do not commit.
.lavish/

# Real API keys. Never commit these. `.env.example` is the committed template.
.env
.env.*
!.env.example

# Editor config
.obsidian/

# Live-API payload cache. Regenerable; do not commit.
artifacts/cache/
